requests
python-dotenv
fastapi
uvicorn[standard]
# THE PROVIDER IN USE. Gemini, on an API key. llm.py imports it lazily, so a
# deployment configured for Claude never loads it.
google-genai>=2,<3
# THE FALLBACK, AND IT IS KEPT ON PURPOSE. LLM_PROVIDER=anthropic points the
# service back at Claude with no code change; the vertex extra pulls in
# google-auth, which is what resolves Application Default Credentials for the
# Vertex path. Neither is reachable today -- Vertex has no Anthropic quota on
# this project and the Anthropic account has no credit -- but the code path is
# exercised by the same tests as Gemini, so it cannot rot unnoticed.
#
# BOTH LINES PIN A MAJOR, and the reason is a live fault this repository already
# hit: `anthropic` was unpinned, and 1.x REMOVED temperature, top_p and top_k
# from messages.create(), which would have broken /parse on the next deploy for
# a reason found nowhere in the repository.
anthropic[vertex]>=1,<2
mcp==1.27.1
tzdata
google-cloud-storage